# Email Service - Gmail API Implementation

## Quick Reference

Put your `credentials.json` file (from Google Cloud Console) in this directory.
After first run, `token.json` will be created automatically.

See `SETUP.md` for detailed setup instructions.
